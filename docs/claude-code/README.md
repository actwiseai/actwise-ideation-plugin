# Install in Claude Code

Add the Actwise marketplace and install the plugin:

```bash
claude plugin marketplace add actwiseai/actwise-ideation-plugin
claude plugin install actwise-ideation@actwise
```

Complete browser authentication when the hosted MCP connection starts. The plugin installs the canonical skill and the production Actwise Ideation MCP endpoint together.
