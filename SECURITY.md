# Security

Report security issues privately to security@actwise.ai. Do not open a public issue for a suspected vulnerability.

This repository must not contain credentials, access tokens, client secrets, private keys, or user data. The plugin connects only to the production Actwise Ideation MCP endpoint declared in `mcp.json` and `.mcp.json`.
